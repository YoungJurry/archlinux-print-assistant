# 更新记录

## 0.5.0

- 打印按钮始终保留，并增加“暂停打印/继续打印”动态按钮。
- 增加“暂停并清空打印队列”按钮及危险操作确认。
- 打印前检查旧任务，可选择丢弃旧任务或按原队列继续打印。
- 打印机被暂停且队列为空时，提交前自动启用打印机。
- 逐个取消当前打印机任务，不使用可能影响其他打印机的 `cancel -a`。
- 检查 CUPS 成功标记，失败任务不再误报为打印完成。
- 修复图片重复嵌套目标纸张造成的额外白边。

## 0.4.1

- “紧急停止当前任务”按钮只在当前 CUPS 任务仍活动时显示。
- 任务完成、取消或离开队列后自动隐藏紧急停止按钮。
- 常规取消失败时才暂停打印机，避免不必要地停用打印队列。

## 0.4.0

- Pacman 包更名为 `archlinux-print-assistant`。
- 中文应用名更名为“arch打印助手”。
- 启动命令、桌面入口、图标、临时目录和 GitHub 仓库同步更名。
- 新包自动替换旧的 `pi-print-assistant` 包。

## 0.3.0

- 拖动项目时增加跟随鼠标的浮动卡片和实时列表换位。
- 增加平滑插值，使拖动过程更加流畅。
- 使用带滞回区间的换位阈值，消除临界位置来回闪烁。
- 修复列表换位后 GTK 拖动偏移突变、项目突然跳到底部的问题。

## 0.2.0

- 改用 XFWM 原生标题栏，修复聚焦时标题栏变白和窗口按钮过大的问题。
- 调整左侧面板边距与最小宽度，避免标题和按钮被裁切。
- 移除上移、下移按钮，支持直接拖动列表项目调整打印顺序。
- 选中待打印项目时，预览自动跳转到该文件或图片的首页。

## 0.1.0

- 首个可安装版本。
- 支持最终 PDF 预览、双面纸张预览、剪贴板图片和多文件合并打印。
